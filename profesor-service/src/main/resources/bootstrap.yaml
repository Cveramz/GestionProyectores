server:
  port: 8082

spring:
  application:
    name: profesor-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8082/
  datasource:
    url: jdbc:postgresql://localhost:5432/profesor-PORTINGESO
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
