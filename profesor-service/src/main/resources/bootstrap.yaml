spring:
  application:
    name: profesor-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
    ddl-auto: update
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      label: main

  datasource:
    url: jdbc:postgresql://localhost:5432/profesor-PORTINGESO
    username: postgres
    password: 1234
